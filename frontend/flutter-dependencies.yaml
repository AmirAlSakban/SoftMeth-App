# Flutter Dependencies for Tutorial Management App
# Add these to your pubspec.yaml under dependencies section

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  provider: ^6.0.5
  shared_preferences: ^2.1.1
  flutter_spinkit: ^5.2.0
  url_launcher: ^6.1.11
  intl: ^0.18.1
  image_picker: ^0.8.7+5
  path_provider: ^2.0.15
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.4.5
  mockito: ^5.4.0
